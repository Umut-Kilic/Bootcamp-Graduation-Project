
@model AdminModel.AssignRoleToUserViewModel

<div class="row" style="min-height:500px">
    <div class="col-md-6 offset-md-3">
        <h2>Kullanıcıya Rol Atama</h2>
        <form asp-action="AssignRoleToUser" asp-route-userId="@ViewBag.userId" method="post">

            
                @*<div class="mb-3 form-check">
                    <input type="checkbox" class="form-check-input" asp-for="@Model[i].Exist">
                    <label class="form-check-label" for="exampleCheck1">@Model[i].Name</label>
                    <input type="hidden" asp-for="@Model[i].Name">
                    <input type="hidden" asp-for="@Model[i].Id">



                    <input type="checkbox" name="SelectedRows" value="" />
                </div>*@

                @foreach (var role in ViewBag.Roles)
                {
                    <div class="form-check">
                        <input type="checkbox" class="form-check-input" name="SelectedRoles" value="@role" id="@role" @(Model.SelectedRoles.Any(i => i == role) ? "checked" : "")>
                        <label for="@role" class="form-check-label">@role</label>
                    </div>
                }

            


            <div class="form-group mt-3">
                <input type="submit" value="Rol Ata" class="btn btn-primary" />
            </div>



        </form>

        <div class="mt-3">
            <a asp-controller="Home" asp-action="UserList">Kullanıcı listesine geri dön</a>
        </div>
    </div>


</div>
